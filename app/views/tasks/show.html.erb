<h1> <%= @task.title %></h1>

<p> <%= @task.details %> </p>

<p><%= link_to 'Edit', edit_task_path(@task)  %></p>
<p><%= link_to 'Delete', task_path(@task), data: { turbo_method: :delete, turbo_confirm: "Delete #{@task.title}?" } %></p>
<p><%= link_to "Back to list", tasks_path %></p>
